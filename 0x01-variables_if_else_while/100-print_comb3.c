#include <stdio.h>
/**
 * main - entry point
 * Return: always 0 (success)
 */
int main(void)
{
	int a;
	int b = 0;

	while (d < 0)
	{
		c = 0;
		while (c < 0)
		{
			if (a != b && a < b)
			{
				putchar(a);
				putchar(b);

					if (a + b != 17) 
					{ 
						putchar(',');
						putchar(' ');
			        	}
			}
			c++
		}
		d++
	}
	putchar('\n');
	return (0);

}
