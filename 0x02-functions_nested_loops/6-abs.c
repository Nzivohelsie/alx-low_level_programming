#include "main.h"
#include <stdio.h>
/**
 * _abs - print absolute number
 * @n: parmeter 1
 * Return: always 0
 */
int _abs(int n)
{

	if (n >= 0)
	{
		n = n;
	}
	else
	{
		n = -(n);
	}
	return (n);
}
